language: python
os: linux
sudo: required
dist: xenial

python:
    - 3.6
notifications:
    email: false


before_install:
    - pip install black
    - pip install future

install:
    - pip install .[tests]

script:
    - black --check .
    - pytest -x
